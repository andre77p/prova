import { Router } from 'express'
import { vilao } from '../repository/VilaoReopository.js'

const server = Router ();


server.get('./vilao/:id', async (req, resp) =>{
    try {
        const voltar
        
    }
})